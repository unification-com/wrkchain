---
- hosts: {{ validator.name }}
  gather_facts: no

  roles:
    - base
    - ethereum
    - node

  tasks:
    - hostname: name={{ validator.name }}
      become: yes

    - name: upgrade all packages
      yum: name=* state=latest
      become: yes

...
