---
- hosts: {{ validator.name }}
  gather_facts: no

  roles:
    - base
    - geth

  tasks:
    - hostname: name={{ name }}
      become: yes

    - name: upgrade all packages
      yum: name=* state=latest
      become: yes

...
