---
- hosts: {{ name }}
  gather_facts: no

  roles:
    - base
    - geth
    - bootnode

  tasks:
    - hostname: name={{ name }}
      become: yes

    - name: upgrade all packages
      yum: name=* state=latest
      become: yes

...
