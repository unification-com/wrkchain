---
- hosts: {{ validator_name }}
  gather_facts: no
  remote_user: {{ home }}

  roles:
    - base
    - geth

  tasks:
    - hostname: name={{ validator_name }}
      become: yes

    - name: upgrade all packages
      yum: name=* state=latest
      become: yes

...
