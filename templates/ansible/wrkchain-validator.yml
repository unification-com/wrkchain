---
- hosts: {{ name }}
  gather_facts: no
  remote_user: {{ home }}

  roles:
    - base
    - geth

  tasks:
    - hostname: name={{ name }}
      become: yes

    - name: upgrade all packages
      yum: name=* state=latest
      become: yes

...
